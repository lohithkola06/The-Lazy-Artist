# The-Lazy-Artist
Train a CNN on a deliberately biased dataset so it “cheats” (e.g., predicts wolf = snow). Use interpretability tools like Grad-CAM to diagnose the shortcut, then apply debiasing “cures” to force the model to learn true visual features and generalize better.
