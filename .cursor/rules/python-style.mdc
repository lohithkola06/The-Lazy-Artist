---
globs: src/**/*.py
alwaysApply: false
---
Applies to `src/**/*.py`.

- Write clear, modular functions; avoid notebook-only logic in src.
- Prefer type hints for public functions.
- Keep functions small; avoid huge monolithic scripts.
- Add basic input validation and helpful error messages.
- Don’t hardcode absolute paths; use config + relative paths.
- No silent failures: raise with context.
- Keep I/O (saving/loading) separate from core logic when possible.
